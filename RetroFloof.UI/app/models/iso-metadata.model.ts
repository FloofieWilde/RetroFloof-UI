export interface IsoMetadata {
    title: string;
    region: string;
    publisher: string;
    developer: string;
    releaseDate: string;
    genre: string;
    emulator: string;
    fullPath?: string;
}